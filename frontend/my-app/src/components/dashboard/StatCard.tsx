import { Box, ShoppingCart, Users, WalletCards } from "lucide-react";

const icons = { violet: WalletCards, blue: ShoppingCart, green: Box, amber: Users, red: Box };
export function StatCard({ label, value, tone = "violet", change, subtext }: { label: string; value: string; tone?: string; change?: number | null; subtext?: React.ReactNode }) { const Icon = icons[tone as keyof typeof icons] ?? WalletCards; const changeText = subtext !== undefined ? subtext : (change === null || change === undefined ? "Current in system" : `${change >= 0 ? "↑" : "↓"} ${Math.abs(change).toFixed(1)}% from last month`); return <div className="dashboard-stat-card"><i className={`metric-icon ${tone}`} aria-hidden="true"><Icon size={20} strokeWidth={2.2} /></i><div className="dashboard-stat-copy"><small>{label}</small><strong>{value}</strong><span className={change !== null && change !== undefined && change < 0 ? "danger-text" : "trend-text"}>{changeText}</span></div><svg className={`stat-spark stat-spark-${tone}`} viewBox="0 0 100 42" preserveAspectRatio="none" aria-hidden="true"><path d="M2 37 C17 34 18 23 31 28 S46 15 57 22 S72 4 82 13 S92 7 98 2 L98 42 L2 42 Z" /><path d="M2 37 C17 34 18 23 31 28 S46 15 57 22 S72 4 82 13 S92 7 98 2" /></svg></div>; }
